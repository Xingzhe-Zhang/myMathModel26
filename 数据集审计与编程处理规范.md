# 数据集审计与编程处理规范

审计日期：2026-09-24；依据同日更新的 `专家问答.txt` 修订。适用范围：`real_attachments/`，供后续四问的数据读取、建模和结果解释使用。**本次未修改任何原始数据文件。**

## 1. 结论与判断口径

《数据说明.pdf》已经明确要求参赛者处理缺失、噪声和冗余，并逐一标注真实、半合成、插值、外推、子集及混合数据。因此，不少“问题”是**有说明的数据设计或自然的数据不完整性**，本身不是文件错误。说明文件明确点出 C8 的 4 个截断 JSON，并建议按目录取最新可解析记录；这很像有意保留的鲁棒读取考点，但文件本身无法证明出题者的主观目的。

**关于 B8 的原审计判断已更正。**本地测量仍显示 B8 与 B6/B7 在相同数值坐标 `(N,D,Q)` 下的 Loss 不同、Q–Loss 趋势相反；但 `专家问答.txt` 明确答复：“B8 是口径不同的半合成分支，不是坏数据”，并允许 B6/B7/B8 三选一或组合。因此，这些数值差异应先视为**分支口径差异**，不能据此判定 B8 文件错误，也不能不经建模说明就把三表拼在一起。

| 总体判断 | 对应现象 |
| --- | --- |
| 明确要求处理，且大体按说明提供 | 半合成/插值/外推/子集、A2/A3 无原文、C8 已知的 4 个截断 JSON；专家又确认 B8 是不同口径的半合成分支。C8 很可能是稳健解析考点，但动机未经证实 |
| 自然产生或整理时常见 | A1 与扩展集重叠、book 域样本少、排行榜重复提交与许可证缺失、模糊匹配覆盖率低 |
| 尚需建模解释或核验 | B8 分支的 Q 定义及 Loss=0.5 堆积机制、C3 历史行中 0 的具体语义、A 的 `sub_path` 全空；这些未明细之处本身不足以证明文件损坏 |

本文对“意图”的判断采用四级：

| 等级 | 含义 | 编程动作 |
| --- | --- | --- |
| 明示设计 | PDF 直接说明性质、缺失或处理方式 | 按说明分层读取，不能称为数据损坏 |
| 自然现象 | 采样、重复提交、公开元数据不全等可以合理解释 | 保留来源和不确定性，按任务处理 |
| 未解释异常 | 与已确认的同一口径定义冲突，PDF 和专家答复均未解释 | 暂停相关联合推断，追溯来源后再决定如何使用 |
| 无法推断动机 | 仅凭文件无法判断是否“故意出题” | 不在论文中断言出题者意图 |

### 1.1 专家答复新增的任务边界

以下是 `专家问答.txt` 的直接解释，优先用于修订本文的**建模与使用建议**；文件行数、缺失数、重叠数等实测事实不因此改变。

1. 问题一的冲突综合评价旨在冲突客观存在时给出**更可信、稳健、可解释的总体评价**，不是降低冲突发生概率。冲突是否直接进入 Q 数值没有硬性规定，须解释取舍。
2. 22 项是原始质量字段口径，不是最终特征数的硬上限。可拆分列表字段或降维，只要保留字段来源、变换和理由。
3. 不存在附件直接给出的“17 维领域质量评分”。A1–A3 的质量文本与 A4–A15 的 RegMix 配比实验**语料库不同、来源独立**；从前者向后者的 17 个训练域传递质量信息，属于需论证的桥接假设。
4. 问题二的 B6/B7/B8 `Q_score` 与问题一构造的质量分数没有现成换算关系；B1/B4/B5 的 Loss 也不是可直接联合回归的同口径目标。题目要求提出**可检验的假设**来统一不同来源规律。

## 2. 审计范围与方法

- 阅读根目录的《数据说明.pdf》（13 页）及 `real_attachments/source_manifest.json`；以实际文件为数值事实来源，按 PDF 第 1 页的优先规则处理说明与文件的差异。
- 逐一用标准 CSV 解析器读取 40 个数据 CSV（不含 `attachment_size_summary.csv`），检查行列宽度、主要缺失、非有限值、关键列重复；检查 A4–A15 的 `index` 对齐、配方和与非负性。
- 流式完整读取 A1–A3 三个 xz JSONL，共 272,505 行，检查 JSON 解析、字段存在、列表长度和空字段。A1–A3 的跨文件身份重叠及字段异常数量同时对照已有只读产物 `Code/outputs/q1/source_audit.csv`、`parse_issues.csv` 和 `Q1/方案评审.md`。
- 解析 C8 的全部 1,958 个逐任务 JSON，按模型目录统计可用性；检查 C1/C2/C3、B6/B7/B8 的关键跨表一致性。
- **覆盖边界与后续补充：**本节原审计只抽查了 A18 前 1,000 行；任务3后续已在 `Code/outputs/q1_task3/regmix_text_evidence.csv` 对 A18 全部 138,034 行完成流式结构审计，并逐域与 A17 的行数、路径和平均字符数核对。该补充仍不能证明文本事实真实性或等价于 22 信号质量评分。C9 原始 Parquet 未独立解码；PDF 将 A18 标为可选，C9 与排行榜 CSV 列为二选一。

## 3. 按附件的发现、性质判断与处理决定

### A：质量信号、领域映射和配比实验

| 发现与实测证据 | PDF 对照及性质判断 | 后续处理决定 |
| --- | --- | --- |
| A1/A2/A3 分别有 51,230 / 17,523 / 203,752 行；全部可解压、逐行解析；八个列表字段的长度均符合各字段固定形状。 | PDF 第 7–9 页给出这三组行数、27/24 字段和 8 个列表字段。专家答复允许合理升维或降维；**22 是原始字段数，不是最终特征数硬约束。** | 流式读取同一套 22 个原始字段；另建版本化特征转换层，记录列表拆分/标量化/降维及其理由。 |
| A1 的 arxiv 有 1,419 条与 A2 重叠；A1 的 github 有 10,000 条与 A3 重叠；重叠处 22 个评分字段一致。合并后唯一 `(域,id)` 为 261,086。 | PDF 第 7 页称 A1 是同一公开源的按域抽样，A2/A3 是扩展，故重叠**自然且可预期**；不是新的独立实验。 | 做文件级统计时保留来源；做样本级拟合、置信区间或训练/验证划分时按 `(domain,id)` 去重，避免泄漏。不能把 A2/A3 当独立复测。 |
| A1 的 book 域只有 171 条，其他多个域各约 10,000 条。 | 按域抽样造成的**样本不平衡**，PDF 已给 A1 总量，但没有保证各域等量。 | 域级估计报告每域 `n` 与区间；域平衡加权要做小样本敏感性分析。 |
| A1–A3 的 `sub_path` 在 272,505 行全部为空。A2/A3 缺 `content`、`_source_domain`、`_source_path`。 | A2/A3 的三个删减字段是 PDF 第 9 页**明示设计**；`sub_path` 全空未明示，属于无效元数据，但不影响主评分。 | 域由文件路径确定；来源记录为 `(source_file,domain,id)`；不依赖 `sub_path`。扩展集不可做依赖原文的语义核验。 |
| 既有解析产物记录 30 条受影响样本、39 个异常字段实例：19 个 ModernBERT 列表内非有限值，20 个字符 2/3-gram 比例越界实例。 | PDF 第 5 页概括数据有缺失与噪声，但没有指定这些坏值。属于**局部自然/未解释异常**，影响小且可定位。 | 逐字段标记为缺失并留原因码；对行级评分设置完整性阈值和敏感性对照。不要把非有限值强行变成 0，也不要无条件删整行。 |
| A4–A15 配方表与相应 Loss 表的 `index` 集合及行序均一致，无负配比和非正 Loss；各配方和为 0.996–1.003。 | PDF 第 9 页说明配比和约为 1，含千分位舍入；**符合说明**。 | 按 `index` 显式连接；建模副本中可按行归一化，记录原始和及调整量。不能依赖当前行序作为永久连接规则。 |
| 既有方案审计显示：A4 的 512 个训练配方均不同；A6/A8/A10 与 A4 没有完全相同的配方。 | 与 PDF 第 2 页标出的训练/检验划分相符；这只排除了**精确配方重复**，不能证明近邻或共同生成机制不存在。 | 保持官方训练/检验划分；不把“无精确重复”扩展解释成完全独立分布。 |
| A12/A14 的 63 个外推配方均与 A4 同 `index` 且整行一致；A13/A15 是外推 Loss。 | PDF 第 2、8 页**明示子集与外推**。这不是新增真实大模型观测。 | 只用于外推情景或外推程序自洽性检查，不用于声称真实 10B/70B 泛化成功。 |
| 17 个配方域中，3 个直接映射、3 个近似映射、11 个在 `domain_mapping_guide.csv` 标为 `(none),inferred`。 | PDF 第 9 页**明示 17 域与 7 个质量域不一一对应**；专家进一步确认 A1–A3 与 A4–A15 是**不同语料库、独立来源**，附件并无现成“17 维领域质量评分”。 | 不能直接把 A1–A3 的域均分当作 A4–A15 的实测质量。任何跨库映射或 `pᵀq` 解释都要列为可检验假设，给出映射依据、未映射域处理及敏感性对照。 |

### B：标度律、半合成质量实验和外推

| 发现与实测证据 | PDF 对照及性质判断 | 后续处理决定 |
| --- | --- | --- |
| B1 Pythia 有 1,176 行，8 个规模各 147 个检查点；B2 Cerebras 有 1,029 行，7 个规模各 147 个检查点。B2 的 `gpu_days`、`step_time_ms`、`grad_norm_avg` 全列空白。 | PDF 第 3、10–11 页将 B1 标为真实主拟合、B2 标为**半合成**族外验证；未承诺 B2 三个运行指标完整。 | B1 主拟合；B2 只能检验对半合成生成机制的稳健性。全空列不插补成硬件实测数据。 |
| B6/B7/B8 分别为 360/450/1,704 行；B7 含 B6 的 360 个完全相同 `(N,D,Q,Loss)` 记录。 | PDF 第 3、10 页称它们是基础、扩展、大规模半合成版本；专家明确允许**三选一或组合**，并说明 B8 为不同口径分支。B6/B7 的包含关系不构成独立复现实验。 | 先声明采用哪个分支及其 Q/Loss 口径。B6+B7 的相同记录只计一次；若组合 B8，加入分支标识、分支特定函数/交互项与可检验的口径转换，不能仅按同名列直接叠行。 |
| **B8 固定 `(N,D)` 后 150/150 组的最高 Q 的 Loss 高于最低 Q；B6 的 45/45 组方向相反。B8 与 B6 在 160 个同数值坐标处 Loss 全不同，与 B7 在 224 个同数值坐标处也全不同；B8 的 362/1,704 行 Loss 恰为 0.5。** | 这些实测差异仍成立；但专家明确回答 **B8 是口径不同的半合成分支，不是坏数据**。相同数字坐标不代表相同实验条件或质量尺度。0.5 堆积的具体生成机制仍未提供。 | **B8 可以选作主分支或对照分支。**分别拟合并报告各分支方向、适用范围和边界值敏感性；组合时先提出并检验尺度/口径假设。不能自行把 `Q` 改为 `1-Q`，也不能用原始拼表的拟合优度证明跨分支一致。 |
| B1、B4、B5 在相近 `N,D` 下的 Loss 可能不同。 | 专家明确回答**不同口径不能直接联合回归**；PDF 第 10–13 页也提示来源与近邻关系。差异不是自动的坏行。 | B1 用于单口径主拟合；B4/B5 先记录验证集、tokenizer、Loss 定义和来源。若要统一规律，声明可检验的来源偏移/尺度假设，并采用按来源分组的检验；无证据时不把 Loss 原值直接拼接。 |
| B3 的 8×500 轨迹是检查点插值，B10 大模型 Loss 是预估，B4 是跨族整理点；B9 的 `FLOPs` 有 11/132 行缺失。 | PDF 第 3、10、13 页**明示插值/估算/真实报告值的区别**，还提示 B4 与训练日志可能有近邻关系。 | 真实 B1 检查点与插值 B3 不作为两个独立证据；B10 只用于情景。B4 盲测需按模型族或近邻分组隔离；B9 缺 FLOPs 不用零代替。 |

### C：排行榜、历史时序、桥接和逐任务评测

| 发现与实测证据 | PDF 对照及性质判断 | 后续处理决定 |
| --- | --- | --- |
| C1 有 4,576 行，六维分数均在 0–100，`Average` 与六维算术均值一致；但 79 个重复 `Model` 名各有不同分数。`Hub License` 缺 1,753 行，提交日期缺 12 行，参数量缺 3 行。 | 排行榜的重复提交、元数据缺失**自然可预期**。PDF 第 12–13 页把 C1 定义为公开榜单清洗版，没有声称 `Model` 唯一。 | 使用模型名、提交日期、评测版本/结果文件组成记录标识；不要仅凭名字覆盖较早记录。缺许可证不得自动判断开源。 |
| C2 的 Epoch 发布日期缺 4,129/4,576 行，组织和开源权重字段各缺 4,133 行。 | `source_manifest.json` 明确称 C2 是与 Epoch AI 的**模糊匹配**增强表；覆盖率低是可解释的，但少数匹配的正确性仍需抽查。 | C2 不适合无条件整体回归；只对已匹配且经规则核验的子集使用 Epoch 字段，并报告匹配率。 |
| C3 有 4,573 条排行榜来源和 26 条 `Historical (papers/reports)` 来源；26 条历史行的 `Average` 全部与同行六维均值相差超过 0.05，其中 18 条至少三项为 0。 | PDF 第 4、12–13 页明确将 C3 标为**混合**并警告跨年评测标准未必相同，但没有解释历史行的 0 是否代表未测。混合设计有说明；把历史行当同一口径则是使用错误。 | 主时序分析只用同口径榜单子集。26 条历史行单独展示；历史 0 标记为“未证实的零值/可能未测”，不直接作为真实零分，也不能用六维值重算一个可比的 `Average`。 |
| C5/C6 桥接表分别 43/75 行；C6 的 `D_tokens_B` 缺 68 行，只有 7 行标为 `High (same model, same validation set)`，68 行标为 `Medium (different validation set, approximate)`。 | PDF 第 12–13 页**明示验证集口径不统一**。 | Loss–Benchmark 桥接先报告 7 条高可比的有限证据，再把中可比行作为单独敏感性分析；训练 Token 缺失不能捏造精确值。 |
| C8 有 1,863 个模型目录、1,958 个 JSON，1,954 个可解析；4 个 JSON 被截断，1,860 个目录至少有一份可解析记录，3 个目录没有可用记录。 | PDF 第 12–13 页**明确写出 4 个截断文件并建议按目录取最新可解析记录**。这是明确的已知脏数据，极可能需要展示稳健读取能力；是否专门为考试制造仍无法证明。 | 按目录按文件时间倒序尝试解析，取最新可解析记录并校验任务字段；写出损坏文件及替代/排除日志。不可把三个无可用记录的目录计为完整评测。 |
| C9 Parquet 与 C1 的核心六维在 PDF 中被定义为等价，C10 的 7 个 Pythia 目录目前只有 README。 | PDF 第 5、12–13 页**明示 C9/C1 二选一、C10 不能替代 C8**。 | 选择一个主排行榜来源；若后续改用 Parquet，需另做列级等价核验。逐任务分析必须读取 C8。 |

四个无法解析的 C8 文件分别位于：

1. `DreadPoor_Winter_Dawn-8B-TIES/results_2025-02-13T18-27-04.338360.json`（所在目录有另一份可解析文件）；
2. `FINGU-AI_Chocolatine-Fusion-14B/results_2025-02-13T18-27-04.338360.json`；
3. `Intel_neural-chat-7b-v3-3/results_2025-02-13T18-27-04.338360.json`；
4. `L-RAGE_3_PRYMMAL-ECE-7B-SLERP-V1/results_2025-02-13T18-27-04.338360.json`。

后三个目录没有其他可解析记录；这一点与总体 1,860/1,863 的目录数相符。

## 4. 建模时最容易产生的错误结论

1. **把模型评分当真值。**A 的 22 个质量字段是自动模型/规则信号，不是逐文本真实质量或边际训练收益。可称“候选质量评价”，需要独立人工核验或下游收益验证。
2. **把冲突消解误写成降低冲突率。**冲突由不同指标对同一文本的评价差异定义；综合模型要在差异存在时给出可信、稳健、可解释的总体评价。是否让冲突强度直接改变 Q 是设计选择，不是赛题硬约束。比较候选方案时应评价评分稳定性、解释性及独立质量证据，不以“标记冲突的比例下降”作为成功标准。
3. **同源泄漏。**A1 与 A2/A3 有身份重叠；B6 完全包含于 B7；A12/A14 的配方全来自 A4。训练/测试划分必须按身份、配方、模型族或数据生成来源分组，而非随机拆行。
4. **把人工生成值当独立实验。**B2/B6–B8、A13/A15、B3/B10 的性质在 PDF 中已标注。用它们证明真实跨尺度迁移或真实 Q 因果作用，会形成循环验证。
5. **把不同 Q 或 Loss 当同尺度。**问题一导出的 Q、B6/B7/B8 的 `Q_score` 没有现成换算关系；B1/B4/B5 的 Loss 也不能直接拼表回归。提出转换、来源偏移或分支模型时，须说明可检验预测与验证数据；没有联合标定数据时只能报告假设范围，不能声称已识别真实换算。
6. **把独立语料库强行关联。**A1–A3 与 A4–A15 来源独立；17 个配比维度不是 17 个已经测得的领域质量。域映射和 `pᵀq` 等构造只是有条件的模型假设。
7. **混合评测口径。**C3 历史行、C6 中可比桥接、不同提交版本不能无标识地拼接；跨年份/跨验证集的差异可能比目标效应更大。
8. **擅自修 B8。**专家确认它是不同口径的有效半合成分支。不能凭方向差异判它为坏数据，也不能凭直觉对 Q 做符号翻转；应先说明分支内含义和跨分支假设。

### 4.1 问题一至问题二的质量接口建议

1. 问题一先基于原始 22 字段构造可复现的特征与候选评分 `Q_A`。允许列表拆分或降维；在输出中同时保存原字段名、变换规则、权重版本和样本/域聚合口径。
2. 将冲突诊断与总体评分分开输出。可比较“不直接惩罚冲突”和“经论证的冲突感知评分”，以独立评价证据决定主用方案；不要为了降低冲突标记率而改评分。
3. A 的 7 个质量域与 RegMix 的 17 个训练域之间仅有参考映射，且语料独立。若使用 `Q_A` 解释配比，先建立域桥接假设并保留未映射域；对映射选择、文档/Token 加权方式做敏感性分析。
4. 问题二在所选 B6/B7/B8 分支内使用其自身的 `Q_score` 拟合半合成质量项。若进一步把 `Q_A` 输入广义标度律，显式定义假设映射 `Q_B=h(Q_A;θ)`。没有成对质量/训练实验时，`θ` 不能由这些附件唯一识别；报告情景范围和可检验条件，不把两种 Q 直接设为相等。

## 5. 后续代码应实现的输入契约

### 5.1 原始数据与派生数据

- `real_attachments/` 只读。清洗、归一化、去重、缓存、诊断表写入独立输出目录，不覆盖原文件。
- 每一条派生记录保留 `source_file`、原始身份键、`source_type`、`transform_version`、`issue_flags`。建议 `source_type` 至少区分 `observed`、`semi_synthetic`、`interpolated`、`extrapolated`、`subset`、`mixed`、`reference`。
- `source_manifest.json` 只有路径、字节数及部分来源备注，没有内容哈希；如需固定复现实验版本，首次运行记录 SHA-256、文件大小与处理配置。

### 5.2 必须自动执行的校验与默认门禁

| 位置 | 校验 | 失败处理 |
| --- | --- | --- |
| A1–A3 | xz/JSONL 逐行解析；22 字段及八个列表长度；嵌套数组内 `NaN`/`Inf`；范围约束；`(domain,id)` 重叠 | 解析失败隔离原行；单字段异常留缺失标记和日志；跨文件同身份冲突应停止合并 |
| A4–A15 | 以 `index` 一对一连接；17 个配比有限且非负、行和接近 1；13 个 Loss 有限且为正 | 键不唯一、缺配对或超出事先设定容差时停止该配对表建模 |
| B1–B8 | 标注来源/半合成分支；`N,D,Q,Loss` 有限且单位一致；跨版本同数值坐标比对；分支内固定 `(N,D)` 的 Q–Loss 方向及 0.5 边界值 | B8 保留为可选分支；未声明可检验的口径转换前不直接拼接 B6/B7/B8。B1/B4/B5 的 Loss 不作无来源校正的联合回归 |
| C1–C3 | 日期、参数量、许可证缺失；模型名重复；来源分层；六维分数范围；`Average` 口径 | C3 历史行不得进入同口径六维主时序；C1 重复名不得静默覆盖 |
| C8 | 每目录按时间排序；解析 JSON；校验目标任务/指标是否齐全 | 记录坏文件，使用最新可解析且满足目标任务的记录；无可用记录则排除模型 |
| C5/C6 | 检查 Loss 验证集可比性与 `D_tokens_B` 缺失 | 主结论按高可比优先，混合样本单独报敏感性 |

### 5.3 主分析与敏感性分析的默认分配

- **主用：**A1–A3 去重后的质量信号、A4/A5 训练配比和 Loss、A6–A11 独立检验、B1 Pythia 实际检查点、C1 同口径榜单、C8 可解析逐任务结果。
- **质量半合成分支：**B6/B7/B8 可三选一或在明确口径假设下组合；B8 不因趋势相反而默认排除。选择哪个分支、质量变量如何解释及 Loss=0.5 的边界敏感性都须报告。
- **有条件使用：**A 的推断域映射、B4/B5 跨族整理点、C2 已核验匹配行、C6 中可比桥接行、C3 历史行；跨来源联合建模前先写可检验假设。
- **只作情景/参考：**A12–A15 外推与子集、B2 半合成轨迹、B3 插值、B10 估算 Loss、C10 README。

## 6. 说明依据与复核入口

| 主题 | 说明出处 | 可复核文件 |
| --- | --- | --- |
| 数据性质、优先规则、读取要求 | `数据说明.pdf` 第 1–6 页 | `real_attachments/source_manifest.json` |
| A 质量样本、配比、字段、域映射 | `数据说明.pdf` 第 7–9 页 | `A_data_value/`、`Code/outputs/q1/source_audit.csv`、`Code/outputs/q1/parse_issues.csv` |
| B 真实/半合成/插值/外推 | `数据说明.pdf` 第 3–4、10–11 页 | `B_scaling_laws/` |
| C 混合时序、桥接、四个坏 JSON | `数据说明.pdf` 第 4–5、12–13 页 | `C_efficiency_evolution/` |
| B8 分支、冲突评分目标、跨题 Q 和 Loss 口径、独立语料 | `专家问答.txt` 的六组问答 | 本文第 1.1、3–5 节 |

后续如取得数据生成脚本或更新版附件，应先重新执行第 5 节校验，再修订本规范。专家答复已澄清 B8 的性质和几处任务边界，但仍未给出 B8 的具体转换公式、Q 的跨附件标定式或历史零值语义；这些不应被本地推断替代。
